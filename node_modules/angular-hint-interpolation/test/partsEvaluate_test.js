var partsEvaluate = require('../lib/partsEvaluate');

describe('partsEvaluate()', function() {
  it('should return the correct grouping of parts based on concatLength', function() {
    var allParts = ['data','text'];
    var originalInterpolation = '{{data.text}}';
    var res = partsEvaluate(allParts, originalInterpolation, scope);
    expect(true).toBe(true)
  });
});